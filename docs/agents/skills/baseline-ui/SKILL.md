---
name: baseline-ui
description: "Vendor (on-demand): Quickly deslop UI code by fixing spacing, hierarchy, typography, and small layout issues. Use when the interface needs a fast cleanup or polish pass. Use for: fast UI cleanup/polish pass (spacing, hierarchy, typography, small layout fixes)."
---

# baseline-ui (vendor skill, on-demand)

> Upstream install name: `baseline-ui` (https://github.com/ibelick/ui-skills.git, pinned b1cc8e0).
> Full reference is `FULL.md` in this folder — read it ONLY when the brief
> matches (fast UI cleanup/polish pass (spacing, hierarchy, typography, small layout fixes)). Otherwise ignore this skill entirely.

1. State a one-line design read first (page kind + audience + vibe).
2. Read `FULL.md` only on a match; apply its dials/rules to the task.
3. Never inject `FULL.md` into always-loaded context.

Update: `./factory.sh skills update baseline-ui && ./factory.sh skills sync <project>`.
