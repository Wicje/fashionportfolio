---
name: ui-skills-root
description: "Vendor (on-demand): Use before UI-related work to select the smallest useful UI Skills context through the ui-skills CLI. Use for: UI work with unclear scope — route to smallest useful UI skill set first."
---

# ui-skills-root (vendor skill, on-demand)

> Upstream install name: `ui-skills-root` (https://github.com/ibelick/ui-skills.git, pinned b1cc8e0).
> Full reference is `FULL.md` in this folder — read it ONLY when the brief
> matches (UI work with unclear scope — route to smallest useful UI skill set first). Otherwise ignore this skill entirely.

1. State a one-line design read first (page kind + audience + vibe).
2. Read `FULL.md` only on a match; apply its dials/rules to the task.
3. Never inject `FULL.md` into always-loaded context.

Update: `./factory.sh skills update ui-skills-root && ./factory.sh skills sync <project>`.
